<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="dev.entao.ui">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>

    <uses-permission android:name="android.permission.ACCESS_DOWNLOAD_MANAGER"/>
    <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION"/>


    <application>
        <activity
                android:name="dev.entao.ui.activities.PageActivity"
                android:configChanges="orientation|keyboardHidden|screenSize|locale|fontScale|layoutDirection|screenLayout"
                android:screenOrientation="portrait"
                android:windowSoftInputMode="adjustPan"/>

        <activity
                android:name="dev.entao.ui.activities.TransPageActivity"
                android:configChanges="orientation|keyboardHidden|screenSize|locale|fontScale|layoutDirection|screenLayout"
                android:screenOrientation="portrait"
                android:theme="@android:style/Theme.Translucent.NoTitleBar"
                android:windowSoftInputMode="adjustPan"/>
        <activity
                android:name="dev.entao.ui.activities.TabBarActivity"
                android:configChanges="orientation|keyboardHidden|screenSize|locale|fontScale|layoutDirection|screenLayout"
                android:screenOrientation="portrait"
                android:windowSoftInputMode="adjustPan"/>

        <activity android:name="dev.entao.util.app.UpgradeActivity"/>

        <receiver android:name="dev.entao.util.app.ApkDownReceiver">
            <intent-filter>
                <action android:name="android.intent.action.DOWNLOAD_COMPLETE"/>
            </intent-filter>
        </receiver>

        <receiver android:name="dev.entao.util.app.NotifyReceiver">
        </receiver>
        <provider
                android:name="dev.entao.util.app.FileProv"
                android:authorities="${applicationId}.files"
                android:grantUriPermissions="true"
                android:exported="false"/>
    </application>

</manifest>